# Minimal inferred dependencies for running the analysis scripts
polars>=0.20
numpy>=1.19,<2.0
matplotlib>=3.3
scipy>=1.5
tensorflow>=2.6
tensorflow-probability>=0.14
tf-keras  # required by tensorflow-probability on TF >= 2.16
astropy>=4.2
emcee>=3.0
pyyaml>=5.4
corner>=2.2
healpy>=1.15
camb>=1.3
ducc0>=0.30

# Development / tooling
pre-commit>=2.20
ruff>=0.0.279
pytest>=7.0
maturin>=1.0  # required to build src/rust_sph (run: make build-rust)